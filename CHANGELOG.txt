5.3.2
 - Added GCC-15.1.0 toolchain as an internal tool
 - Made avrdude 8.1 the standard version
 - ATTinyCore removed
 - TinyCore 0.8.0 added
 - All board manifests have been extended by a SVD file located in pyavrocd-util/svd
 - All boards w/o a debug interface have been marked with 'pyavrocd: nodebug'
 - Added PyAvrOCD 1.5.3 as an internal tool
 - Added pyavrocd as a potential debug tool
 - Set up the configuration for this tool
 - Fixed the size target in the Platform targets

5.3.3
 - Added T2313a and T26 in boards
 - Added a dummy folder misc/svd with a dummy file so that the SVD files can be accessed
 - Added a 'Disable debugWIRE' target

5.3.4
 - Fixed Problem in dw_disable target
 - Fixed specification of pyavrocd version spec

5.3.5
 - replaced dummy.txt containing the word 'dummy' in misc/svd with the empty file 'dummy' in order to fix the endless SVD file loading problem
 - added "default" field to all debug tools
 - changed debug option handling: One now can overwrite tools fields in the board files
 - changed recognition that a mcu is pyavrocd debuggable: Now "pyavrocd_target" is similar to simavr_target
 - added xmini boards
 - added 'monitor disconnect' before 'detach' in
   'pio_reset_run_target' so that the debugger can shut down before the
   GDB server gets terminated

5.3.6
 - Switch back to toolchain 7.3.0 (added AVR-GDB and mainfests to
   Spence Konde's version)

5.3.7
 - Placed SVD files into their 'default' location misc/svd/ and
   changed all board manifests accordingly

5.3.8
 - Made pyavrocd a mandatory package
 - Set GDB in builder/main.py to avr-gdb in pyacrocd package
 - Using again standard AVR-GCC toolchain 3.70300.220127